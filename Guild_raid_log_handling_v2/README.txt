Changes from previous version:
- The automatic log handler stores all logs into a single json file 
(rather than several ones, like I was doing when I was copypasting them myself)
and Guild_raid_processor.py was modified to account for this.
- Colors are now added to the Morale Points column. 